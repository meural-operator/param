# =============================================================================
# Ramanujan@Home — Python Dependencies
# =============================================================================
# This file lists ALL pip-installable dependencies for the project.
#
# INSTALL (CPU only):
#   pip install -r requirements.txt
#
# INSTALL (NVIDIA GPU / CUDA):
#   pip install -r requirements-cuda.txt
#
# For the full recommended setup, see INSTALL.md or use:
#   conda env create -f environment.yml
# =============================================================================

# ---------------------------------------------------------------------------
#  Core Mathematics & Numerics
# ---------------------------------------------------------------------------
mpmath==1.3.0               # Arbitrary-precision arithmetic (GCF evaluation)
numpy>=2.0.0                # Tensor / array computation
scipy>=1.14.0               # Advanced numerical solvers, optimization
sympy>=1.13.0               # Symbolic mathematics (identity verification)

# ---------------------------------------------------------------------------
#  Machine Learning & RL
# ---------------------------------------------------------------------------
scikit-learn>=1.5.0         # Classical ML utilities (feature engineering)
gymnasium>=1.0.0            # RL environments (GCF reward env, MCTS agent)
tensorboard>=2.16.0         # Training visualization

# ---------------------------------------------------------------------------
#  Symbolic Regression (Optional — heavy dependency)
# ---------------------------------------------------------------------------
pysr>=1.0.0                 # Symbolic regression via genetic programming

# ---------------------------------------------------------------------------
#  Distributed Compute & Networking
# ---------------------------------------------------------------------------
requests>=2.31.0            # HTTP client for edge node ↔ server handshakes
pyrebase4>=4.8.0            # Firebase Realtime Database client
firebase-admin>=6.5.0       # Firebase Admin SDK (server-side coordination)
google-cloud-firestore>=2.16.0  # Firestore client (work unit management)

# ---------------------------------------------------------------------------
#  Search & Optimization
# ---------------------------------------------------------------------------
ortools>=9.8                # Google OR-Tools (constraint programming)
pybloom_live>=4.0.0         # Probabilistic bloom filters (deduplication)

# ---------------------------------------------------------------------------
#  Utilities
# ---------------------------------------------------------------------------
tqdm>=4.65.0                # Progress bars
pyyaml>=6.0                 # YAML config parsing
semver>=3.0.0               # Semantic versioning

# ---------------------------------------------------------------------------
#  PyTorch (CPU-only fallback)
#  For CUDA support, use requirements-cuda.txt instead
# ---------------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.4.0
torchvision>=0.19.0
